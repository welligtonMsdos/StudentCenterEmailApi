namespace StudentCenterEmailApi.src.Application.DTOs;

public record UserDto(string _id,
                      string Name,
                      string Email,
                      string PassWord);
